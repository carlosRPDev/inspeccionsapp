<% if @person.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @person.id %>').replaceWith(' <%= j render "row", person: @person %>');
  $('.alert-success').html("La persona ha sido editada con éxito").show();
  $('#people-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @person.errors.full_messages[0] %>');
<% end %>
