<% if @visit.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @visit.id %>').replaceWith(' <%= j render "row", visit: @visit %>');
  $('.alert-success').html("La visita ha sido editada con éxito").show();
  $('#visit-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @visit.errors.full_messages[0] %>');
<% end %>
